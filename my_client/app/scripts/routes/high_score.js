/*global define*/
define([
    'jquery',
    'backbone'
], function($, Backbone) {
    'use strict';

    var HighScoreRouter = Backbone.Router.extend({
        routes: {}

    });

    return HighScoreRouter;
});